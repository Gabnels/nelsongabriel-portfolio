<section data-aos="fade-up" class="pt-10 pb-28">
  <!-- Heading -->
  <div class="text-center mb-16">
    <span
      class="inline-block px-4 py-2 rounded-full bg-[var(--color-primary)]/10 text-[var(--color-primary)] text-sm font-medium mb-5"
    >
      Prueba social
    </span>

    <h2 class="text-4xl md:text-5xl font-bold mb-6">
      Resultados que generan
      <span class="text-[var(--color-primary)]"> confianza</span>
    </h2>

    <p class="text-neutral-400 max-w-2xl mx-auto text-lg leading-relaxed">
      Estrategias enfocadas en posicionamiento, visibilidad y conversión para
      negocios modernos.
    </p>
  </div>

  <!-- GRID -->
  <div class="grid md:grid-cols-2 gap-8">
    <!-- TESTIMONIO -->
    <div
      data-aos="fade-up"
      data-aos-delay="100"
      class="group relative p-8 rounded-3xl bg-neutral-900/70 border border-neutral-800 overflow-hidden
             hover:border-[var(--color-primary)] transition duration-300
             hover:shadow-[0_0_40px_rgba(34,197,94,0.12)]"
    >
      <!-- Glow -->
      <div
        class="absolute inset-0 bg-gradient-to-br from-[var(--color-primary)]/5 to-transparent opacity-0 group-hover:opacity-100 transition"
      >
      </div>

      <!-- Stars -->
      <div
        class="flex items-center gap-1 mb-6 text-[var(--color-primary)] text-lg"
      >
        ★ ★ ★ ★ ★
      </div>

      <!-- Quote -->
      <p class="relative text-neutral-300 text-lg leading-relaxed mb-8">
        “Aumentamos nuestras visitas orgánicas más de un 200% y comenzamos a
        recibir clientes constantemente desde Google.”
      </p>

      <!-- Footer -->
      <div class="flex items-center gap-4">
        <!-- Avatar -->
        <div
          class="w-14 h-14 rounded-full bg-[var(--color-primary)]/15 border border-[var(--color-primary)]/20 flex items-center justify-center text-[var(--color-primary)] font-bold text-lg"
        >
          MR
        </div>

        <div>
          <h3 class="font-semibold text-white">Miguel Rodríguez</h3>

          <p class="text-sm text-neutral-500">Restaurante Local</p>
        </div>
      </div>
    </div>

    <!-- TESTIMONIO -->
    <div
      data-aos="fade-up"
      data-aos-delay="200"
      class="group relative p-8 rounded-3xl bg-neutral-900/70 border border-neutral-800 overflow-hidden
             hover:border-[var(--color-primary)] transition duration-300
             hover:shadow-[0_0_40px_rgba(34,197,94,0.12)]"
    >
      <!-- Glow -->
      <div
        class="absolute inset-0 bg-gradient-to-br from-[var(--color-primary)]/5 to-transparent opacity-0 group-hover:opacity-100 transition"
      >
      </div>

      <!-- Stars -->
      <div
        class="flex items-center gap-1 mb-6 text-[var(--color-primary)] text-lg"
      >
        ★ ★ ★ ★ ★
      </div>

      <!-- Quote -->
      <p class="relative text-neutral-300 text-lg leading-relaxed mb-8">
        “Ahora aparecemos en Google Maps y empezamos a recibir solicitudes
        reales desde búsquedas locales.”
      </p>

      <!-- Footer -->
      <div class="flex items-center gap-4">
        <!-- Avatar -->
        <div
          class="w-14 h-14 rounded-full bg-[var(--color-primary)]/15 border border-[var(--color-primary)]/20 flex items-center justify-center text-[var(--color-primary)] font-bold text-lg"
        >
          CD
        </div>

        <div>
          <h3 class="font-semibold text-white">Clínica Dental Sonrisa</h3>

          <p class="text-sm text-neutral-500">SEO Local</p>
        </div>
      </div>
    </div>
  </div>
</section>
